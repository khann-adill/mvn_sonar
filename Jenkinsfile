pipeline {
   agent any
	stages {
      stage('Git Checkout') {
         steps {
            git 'https://github.com/khann-adill/parking_backend.git'
		}
	}
    stage('test') {
      steps {
        sh 'echo hi.."
      }
    }
}
}
